name: Actions_Pipeline

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

env:
  IMAGE_NAME: image-angular-react-starter
  GH_TOKEN: ${{ github.token }}

jobs:
  Build:
    runs-on: ubuntu-latest

    steps:
      - name: Get repository code
        uses: actions/checkout@v3

      - name: Installing packages Angular
        run: cd ${{ github.workspace }}/angular && npm install
      - name: Installing packages React
        run: cd ${{ github.workspace }}/react && npm install

#      - name: Run test
#        run: cd ${{ github.workspace }}/angular && npm run test
#      - name: Error
#        if: failure()
#        run: exit 1

      - name: Build project
        run: cd ${{ github.workspace }}/angular && npm run build

      - name: Build Docker image
        run: sudo docker build . -t $IMAGE_NAME

      - name: Login to GitHub container Registry
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Post image to github packages
        run: |
          IMAGE_ID=ghcr.io/krawczowakris/$IMAGE_NAME
          VERSION=latest
          docker tag $IMAGE_NAME $IMAGE_ID:$VERSION
          docker push $IMAGE_ID:$VERSION

#      - name: Send email about pipeline status
#        uses: dawidd6/action-send-mail@v3
#        with:
#          server_address: smtp.yandex.ru
#          server_port: 465
#          secure: true
#          username: ${{ secrets.MAIL_USERNAME }}
#          password: ${{ secrets.MAIL_PASSWORD }}
#          subject: Github Actions job result
#          to: ${{ secrets.FIRST_EMAIL_ADDRESS }}
#          from: KrawczowaKris
#          body: Build job of ${{ github.repository }} completed with status ${{ jobs.status }}

      - name: Get pipeline execution time
        id: time
        run: gh run list --workflow ci.yaml | head -n 1 | awk -F'\t' '{ print $8 }'

      - name: Send email about time
        run: echo "Pipeline exe time ${{ jobs.status }}"
#        uses: dawidd6/action-send-mail@v3
#        with:
#          server_address: smtp.yandex.ru
#          server_port: 465
#          secure: true
#          username: ${{ secrets.MAIL_USERNAME }}
#          password: ${{ secrets.MAIL_PASSWORD }}
#          subject: Github Actions time
#          to: ${{ secrets.FIRST_EMAIL_ADDRESS }}
#          from: KrawczowaKris
#          body: Pipeline execution time ${{ jobs.status }}
